require "station"
describe Station do
    it " has a class station " do
    expect(subject).to be_kind_of(Station)
    end
    # it "has a method zone " do
    #     expect(subject).to respond_to(:zone)
    # end 
    # it "has a method name " do
    #     expect(subject).to respond_to(:name)
    # end

end 